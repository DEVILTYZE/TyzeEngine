namespace TyzeEngine;

public enum ArrayType
{
    Unknown = 0,
    OneDimension = 1,
    TwoDimensions = 2,
    ThreeDimensions = 3,
    FourDimensions = 4,
}

public enum VisibilityType
{
    Visible = 0,
    Hidden = 1,
    Collapsed = 2
}

public enum BodyVisualType
{
    Color = 0,
    Texture = 1,
    ColorAndTexture = 2
}